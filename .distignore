# Repo metadata
.git/
.github/
.gitattributes
.gitignore

# Build & analysis tools
phpcs.xml.dist
phpstan.neon
phpstan.neon.dist

# Node / JS deps
node_modules/
package.json
package-lock.json
yarn.lock

# PHP deps (if bundled)
vendor/
composer.lock
composer.phar

# Test suites & coverage
tests/
coverage/
phpunit.xml*
*.test.*

# Editor / IDE cruft
.vscode/
.idea/
.DS_Store

# Local / secrets
.env
*.log

# Misc artifacts
*.dist
distignore
